fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
PyJWT[crypto]>=2.8.0
httpx>=0.26.0
python-dotenv>=1.0.0

# PostgreSQL (Phase 10 production hardening)
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
sqlalchemy[asyncio]>=2.0.0
alembic>=1.13.0
pgvector>=0.3.0

# Redis (Phase 10 — context caching)
redis[asyncio]>=5.0.0

# Rate limiting (Phase 10)
slowapi>=0.1.9

# Background scheduler (Phase 9a — grievance deadline monitoring)
apscheduler>=3.10.0
# Business-day calculation (Phase 9a)
holidays>=0.46

# Test dependencies
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-httpx>=0.28.0
